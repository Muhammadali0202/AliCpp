//Check if an array is a subset of another array.
#include <iostream>
#include <vector>
using namespace std;
int main(){
    vector<int> v1 = {1, 2, 3, 4, 5, 6};
    vector<int> v2 = {1, 2, 3};
    int n = v1.size();
    int m = v2.size();
    for(int i = 0; i<m; i++){
       .
}
}