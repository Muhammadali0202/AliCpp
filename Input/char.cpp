#include <iostream>
using namespace std;
int main(){
    // char ch = 'a';
    // cout << ch;
    char ch = 'z';
    cout << (int)ch; // => 122 Which is the ASCII value of the alphabet z. and this is one of the method to find ASCII value of a character.
    // A == 65 a == 97 0 == 48
        return 0;
}