#include <iostream>
#include <cmath>
using namespace std;
int fun(int x,int y){
    int sum = x +y;
    return sum;
}
int mini(int x, int y){
    int a;
    if(x<y) a=x;
    else a=y;
    return(a);
}
int main(){
    // int x = 2;
    // int y = 9;
    // cout << fun(x,y);
    // int x,y;
    // cin >> x >> y;
    // cout << min(x,y);
    // cin >> x >> y;
    // cout << min(x,y);
    cout << sqrt(7);
}